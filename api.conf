[supervisord]
nodaemon=true

[program:api]
directory=/app
command=/bin/bash -c '. /app/start_app.sh start'
autostart=true
autorestart=true
stderr_logfile=/app/app.err.log
stdout_logfile=/app/app.out.log

[supervisorctl]
serverurl=unix:///var/run/supervisord.sock
